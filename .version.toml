[main]
  extraCommands = ["git push", "git push --tags"]
  serialize = "{version}-{banner}"
  tag = true
  version = "1.1.0"

[[operate]]
  location = "cmd/hackbox/hackbox.go"
  replace = "__version__ = \"{}\""
  search = "__version__ = \"{}\""